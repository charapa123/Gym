version: 2

models:
  - name: Answers_model
    columns:
      - name: response_id
        tests:
          - not_null

  - name: Questions_model
    columns:
      - name: itemid
        tests:
          - not_null

  - name: GYM_UPDATE
    description: "Pivoted answers with question titles as columns"
    columns:
      - name: response_id
        tests:
          - unique
          - not_null
